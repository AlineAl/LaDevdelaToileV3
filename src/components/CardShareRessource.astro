---
interface Props {
    title: string;
    author: string;
    description: string;
    path: string;
}

const { title, author, description, path } = Astro.props;

---

<div class="col-span-12 lg:col-span-9">
    <div class="grid md:grid-cols-3 ltablet:grid-cols-3 lg:grid-cols-3 gap-x-6 ptablet:gap-x-4 gap-y-16 pt-10">
        <a href={path} class="relative rounded-lg mt-8 hover:scale-105 hover:transition cursor-pointer border border-muted-200 bg-white dark:bg-muted-900 transition-all duration-300 p-4" :class="'p-4'">
            <div class="font-sans mt-3">
                <h4 class="font-semibold text-xl">
                    { title }
                </h4>
                <p class="text-sm text-muted-400">
                    Par { author }
                </p>
            </div>
            <div class="mt-2">
                <p class="text-sm ptablet:text-xs text-muted-500 dark:text-muted-400">
                    { description }
                </p>
            </div>
        </a>
    </div>
</div>